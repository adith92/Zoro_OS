import { UniversalToolPage } from '@/components/modules/UniversalToolPage';
import { Search } from 'lucide-react';

export function SearchUtility() {
  return <UniversalToolPage category="search" title="Search Utility" icon={Search} />;
}
